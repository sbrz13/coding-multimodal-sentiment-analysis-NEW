{
    "data_config": {
        "batch_size": 8,
        "num_workers": 0
    },
    "train_config": {
        "learning_rate": 2e-5,
        "num_epochs": 10,
        "early_stop_patience": 3,
        "device": "cpu"
    },
    "model_config": {
        "projection_dim": 256,
        "attention_dim": 64,
        "dropout_rate": 0.5,
        "freeze_encoders": true
    }
}
